.deno/
.vscode/.deno/
.env
# DB
data/*.db
data/*.db-wal
data/*.db-shm
data/typegraph.db/
# Runtime
config/config.json
node_modules/
run/
logs/
.ursamu.pid
.ursamu-deno.pid

# Local-only engine vendor (prod always uses JSR pins in deno.json)
vendor/mush/
.deno.local.json



# Live wiki content (editor + prod). Never reset by deploy.
# Seed defaults live in wiki.sample/ (tracked).
wiki/

.jwt_secret
